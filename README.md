`generate-markdown-file.js` has functions that parse through all `spec` files & extract test names (values after `describe` & `it`).

It is used in the `Generate Markdown File with Test Names` Github Action.